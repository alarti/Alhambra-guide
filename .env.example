# -----------------------------------------------------------------------------
# Supabase Configuration
# -----------------------------------------------------------------------------
# Estas son las claves públicas y seguras para usar en el frontend.
# Copia los valores desde el panel de tu proyecto en Supabase:
# Settings > API
#
# No es necesario crear un archivo .env para este proyecto, ya que las claves
# se insertarán directamente en el archivo app.js. Este archivo sirve
# únicamente como recordatorio de las variables que la aplicación necesita.
# -----------------------------------------------------------------------------

SUPABASE_URL="https://<ID-DE-PROYECTO-SUPABASE>.supabase.co"
SUPABASE_ANON_KEY="<TU-CLAVE-ANON-PUBLICA>"
